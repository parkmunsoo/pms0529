package Chapter23;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

class Student {

	String name;
	String depart;
	int st_id;
	Double grade;

	public Student( String name, String depart, int st_id, Double grade) {
		this.name = name; // KEY
		this.depart = depart; // VALUE
		this.st_id = st_id;// VALUE
		this.grade = grade;// VALUE
		  
	}


	public String toString() {
		return name + depart ;
	}
}
public class Q4 {
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		
		Scanner sc = new Scanner(System.in); // Scanner 객체 생성

		List<Student> student_list = new ArrayList<>();
		String Input_data = sc.nextLine();

		
		
		String name = name.get(Input_data);
		String depart = depart.get(Input_data);
		String st_id = st_id.get(Input_data);
		String grade = grade.get(Input_data);

		for(int i =0; i<=4; i++) {
			student_list.add(new Student("1")); 

		}
	}

}
