package Ex03;
import java.util.Scanner;
@FunctionalInterface
interface Test {
    void add(int a, int b);
}

@FunctionalInterface
interface Three {
    void add1(int last_num1);
}
@FunctionalInterface
interface five {
    void add2(int last_num2);
}
public class pra0501 {
    public static void main(String[] args) {
        // TODO Auto-generated method stub
        Test test = (a, b) -> {
            int c = 10;
            System.out.println(a + b + c);
            System.out.println("a+b의 값은 " + (a + b));
        };
        test.add(5, 10);
        	
        Three test1 = (last_num) -> {
        	//  값저장공간
            int result = 0;
            for (int i = 0; i <= last_num; i++) {
                if (i % 3 == 0) {
                    result += i;
                }
            }
            System.out.println("3의배수의합:" + result);
        };

        test1.add1(100);
    }
}